> DATA = get.sample()
> names(DATA)
[1] "locs"   "vars"   "z"      "obs"    "priors"
> Z <- array(DATA$obs, c(10000,2))
> Z <- aperm(Z, c(2,1))
% as.vector() takes column-wise, hence the whole transpose business
% aperm() is a generalized transpose operation for arbitrary-dimension arrays.
> Y <- as.vector(Z)
> writeBin(Y, "obs.30means.10K.data", size = 4, endian = .Platform$endian)
> centers <- read.table("30means.10K.fuzzyOutput.Centers.txt")
% plot results.
> jpeg('Center.jpg')
> plot(DATA$obs)
> points(centers, col="red")
> points(DATA$locs[1:10,], col="white", pch=4)
> points(DATA$locs[11:20,], col="grey", pch=4)
> dev.off()
null device
          1
> 
